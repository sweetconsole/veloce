import { type FC } from "react"
import styles from "./DefaultHeader.module.scss"

const DefaultHeader: FC = () => {
	return <div className={styles.DefaultHeader}>DefaultHeader</div>
}

export default DefaultHeader
